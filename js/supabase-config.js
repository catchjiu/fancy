// Paste your Supabase project URL and anon key from Dashboard → Settings → API.
// The anon key is safe to expose in the browser; it is restricted by Row Level Security.
window.__CATCH_SUPABASE__ = {
    url: '',
    anonKey: ''
};
